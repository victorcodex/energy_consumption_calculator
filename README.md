# basic-energy-consumption-app
A basic demo energy consumption calculator application built with React and Node.js(Express).

### `npm install` (repeat in client folder)
### `npm run client-install`
### `npm run dev`
